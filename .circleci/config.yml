version: 2
jobs:
  build:
    working_directory: ~
    machine: true
    steps:
      - checkout
      # hugo install
      - run: 
          command: 'go get -u -v github.com/spf13/hugo'
      # hugo build
      - run:
          command: 'hugo -D'
      - run:
          command: 'ls -l'
      - save_cache:
          key: cache-{{ epoch }}
          paths:
            - ~
  deploy:
    working_directory: ~
    machine: true
    steps:
      - restore_cache:
          keys:
            - cache
      # run rsync
      - run:
          command: 'pwd'
      - run:
          command: 'ls -l'
workflows:
  version: 2
  build_and_test:
    jobs:
      - build:
          filters:
              only: master
      - deploy:
          requires:
            - build
          filters:
            branches:
              only: master
